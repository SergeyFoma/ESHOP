pip list
django-admin
python manage.py

подключение venv через manage.py 31-20

PS C:\Users\Fomenko.SM\Eshop> Set-ExecutionPolicy -Scope CurrentUser

-----------settings.py---------------
STATIC_URL = 'static/' #префикс
STATICFILES_DIRS=[         #путь
    BASE_DIR / 'static'
]
STATIC_ROOT #нужен для деплоя

___________________________
python manage.py debugsqlshell

______________Q________________
x = Products.objects.filter(Q(name__contains="диван")|Q(description__contains="диван"))